---
description: Java后端通用开发基础规范
when:
  files: ["src/**/*.java", "sql/**/*.xml", ".cursor/**/*.md"]
priority: 5
---
# 通用编码规范
1. 所有改造遵循开闭原则，新增功能通过扩展/开关实现，不修改原有业务代码；
2. 新增逻辑必须配套日志打印：入参、执行结果、异常堆栈；
3. 数据库操作禁止硬编码，常量统一抽取；
4. 代码改动必须先设计方案，再编码实现，最后补充单元测试。
# AI协作规范
1. 复杂需求先输出结构化设计方案，确认后再编写代码；
2. 分步执行，每一步完成后主动汇总当前改动；
3. 输出代码附带关键注释，说明适配一户多股东场景的改动点。